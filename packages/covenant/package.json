{
  "name": "@therecord/covenant",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Covenant — obligations that were owed, and provably were not performed.",
  "scripts": {
    "scan": "tsx src/scan.ts",
    "render": "tsx src/render.ts",
    "deploy:local": "tsx src/deploy.ts",
    "e2e": "tsx src/deploy.ts && tsx src/publish.ts",
    "build": "pnpm run scan && pnpm run render",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": { "viem": "^2.21.0" },
  "devDependencies": { "@types/node": "^22.10.0", "tsx": "^4.19.0", "typescript": "^5.9.3", "vitest": "^2.1.0" }
}


