{
  "name": "@therecord/procedure",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Procedure \u00e2\u20ac\u201d the practitioner is a reproducible program, and it can refuse to sign.",
  "scripts": {
    "run": "tsx src/run.ts",
    "render": "tsx src/render.ts",
    "e2e": "tsx src/publish.ts",
    "build": "pnpm run run && pnpm run render",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "redrun": "tsx src/redrun.ts",
    "backfill": "tsx src/backfill-run.ts",
    "render:backfill": "tsx src/render-backfill.ts",
    "verify": "tsx src/verify-pack.ts",
    "spec": "tsx src/emit-faults.ts"
  },
  "dependencies": {
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.9.3",
    "vitest": "^2.1.0"
  }
}
