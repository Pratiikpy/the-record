{
  "name": "@therecord/doctor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "doctor — why is this TEE machine not working? Deterministic diagnosis, optional prose.",
  "scripts": {
    "doctor": "tsx src/cli.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "render": "tsx src/render.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.9.3",
    "vitest": "^2.1.0"
  }
}
